// tailwind.config.ts — Tailwind CSS configuration
//
// Key things to set up here:
//   - `content`: tell Tailwind which files to scan for class names
//     so it can purge unused styles in production build
//     → ["./index.html", "./src/**/*.{ts,tsx}"]
//   - `darkMode`: set to "class" if you want manual dark mode toggle,
//     or "media" to follow the OS preference
//   - `theme.extend`: add custom colors for the race UI
//     (e.g., neon green for gains, red for losses, dark track background)

// TODO: implement
export default {}
