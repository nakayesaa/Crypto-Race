// postcss.config.js — PostCSS plugins for processing CSS
//
// Tailwind CSS works as a PostCSS plugin, so we need this file.
// autoprefixer adds vendor prefixes automatically.
// No custom logic needed here — just load the plugins.

// TODO: implement
export default {}
